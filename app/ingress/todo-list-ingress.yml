apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: my-todo-ingress
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: my-todo-service
          servicePort: 80